{
  // "extends": "../../tsconfig.json", /* 需要继承的话，通过 tsc --showConfig 可以查看合并后的配置 */
  "compilerOptions": {
    "baseUrl": "." /* 当前目录 */,
    "target": "esnext",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts"] /* 对哪些文件进行处理 */
}
